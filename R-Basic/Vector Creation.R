# Vector of string
days <- c("Monday","Tuesday","Wednesday")

# Print days
days

# Write a R program to create a vector of patient ID, Age, Gender and affacted 
# diseases. Display the data types of every vector with length 
patient_id <- c(1,2,3,4,5)
age <- c(12.5,24.1,89,25.6,35)
affected_disease <- c("Corona","Corona","Heart-Disease","Cancer","Blood Pressure")

# Check datatype and length
class(patient_id)
length(patient_id)

class(age)
length(age)

class(affected_disease)
length(affected_disease)
